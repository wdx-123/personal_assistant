

8、思考现阶段，刷题曲线，改如何设计？
把代码看完

9、把campub中的，加密代码抽取到这里，可以加密与双向存储/解密获取


10、让AI模仿 力扣/洛谷 给一版本设计。蓝桥杯的设计模板代码
1、需要对（手机号+密码）进行联合哈希的那种--用于标识个人的唯一性
2、蓝桥的注册方式是（手机号/密码）进行注册的。
3、其他的都模仿 洛谷/力扣 进行设计
4、如果infrastr...中，多次返回异常，则自动关闭蓝桥杯的爬虫定时任务（这个可以放入redis中，可过期----着这是为了避免，修改了账号密码，从而导致无用的爬虫爬取）

 




## 后期需补充文档
后期还需要补充文档
1、事件驱动架构-（redis stream+outbox+双通道）-docs文档
2、infrastructure - LeetCode / Luogu / Lanqiao - 第三方集成的文档
3、可观测性平台 - 全链路追踪文档
4、可观测性平台 - 指标文档
5、熔断/限流 - docs文档
6、排行榜设计-读/写模型文 - docs文档
7、框架整体设计 - docs文档

## 以完成
1、修改接口/system/user/list，为何获取不到用户数据（如果不存在则新建，否则修改）

2、加入团队（邀请码）新增这样的接口

3、踢出团队的接口
    1、现阶段我准备重构User在组织/账号的这一部分
    首先新增（自己主动退出组织/被踢出组织/修改组织内被分配的角色/主动注销自己的账号-其实就相当于被禁用）
    账号级别：（禁用/启用）（定时可清理，禁用的之后，就不在参与排名什么的）

4、新增《全体成员》这一组织，

补充：
RecoverMember/...进行踢人/拉人/删除/冻结...等操作的时候，除了硬编码（超级管理员/组织所有者），还要进行操作者的角色权限判断，他拥有的角色中，在本组织内，到底有没有操作权限（具体的权限）你认为这样设计合理嘛？（canManageOrgMember...）
已落地：
组织域写操作统一通过 `AuthorizationService.AuthorizeOrgCapability` 做资源级授权；`owner / super_admin` 直通，其余按 `userID@orgID` 的 capability 判断。当前已替换 `UpdateOrg / DeleteOrg / AssignRole / KickMember / RecoverMember` 的硬编码服务层判断，并收口到统一授权入口。

中间件进行优化，采用redis进行优化，避免每次查询都会直接打在db上。
可以将org_member的内容，


5、修改注册的方式：

用户登录的时候，需要同步添加邀请码，
首先会分配到指定团队，并且在user下，设计到org_id
默认分配到 《全体成员》上，为用户新增这一组织。并且是默认的普通成员。

6、目前的排名系统基于 Redis Sorted Set（ZSet）实现，但是仍然要经历重构
    1、首先针对 luogu 抛弃之前的设计方案
        1、首先会对全体成员维护一个总的redis soreted set的排名
        2、对每一个组织都进行一个排名的维护
        3、当用户切换当前组织的时候，删除旧的组织内的zset，并添加到新的组织所维护的zset内
    3、同时 luogu/力扣 都是要支持全量重建的
    4、同时建立hash，用来存储用户的详细信息。（组织/姓名/头像->力扣的/洛谷的...这种情况下是让返回的函数共用的
    5、关于 luogu/力扣 的具体信息的更新 统一由 事件异步更新 (比如切换组织，被踢出组织，新增组织，或者刚刚注册...)
    6、现在统一把 原本所有需要同步更新力扣/洛谷的 直接操作的 给删除掉。统一采用事件发布/订阅，进行异步操作。包括对应的，需要异步操作的其他内容(对用户注销/禁用/恢复/踢出组织/....比如更新casbin啊、redis存的状态呀...)，不要去污染主业务！
    同时可以优化 activeUserMW 中间件的设计，由于是通过异步会进行修改，所有ttl的随机刷新时间 与 过期时间可以放长一点。


7、请将权限的相关操作，下移动到pkg中，包括权限的检查啥的，从而防止service之间相互调用，你觉得如何？还是你有更好的建议。
（现状已调整为：`pkg/casbin` 只保留纯权限引擎能力；`AuthorizationService` 负责 owner / super_admin / capability 等业务授权；`PermissionProjectionService` 负责 Casbin 全量重建、主体修复、权限事件消费与 reload 广播。业务 Service 不再直接碰 `Enforcer`，图谱变更统一走 DB + outbox + 异步重建。）

7.1、把锦豪的需求实现。
