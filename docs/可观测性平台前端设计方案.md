设计方案：记得后期ai同步更新最后的内容

## 链路返回完善
1、完善查询链接
/system/observability/traces/query 查询只返回 root 摘要，按 trace_id+request_id 聚合，不再按 span 明细返回。默认 root_stage=http.request；当前端显式传 root_stage 时，可切换为 task 或 all。返回字段为最表层信息 + 统计字段（span_total、error_span_total）+ root 类型字段（stage、kind），用于列表展示。

2、详情接口已合并为：
/system/observability/traces/detail/{id}?id_type=trace|request
- id_type=trace：按 trace_id 查 span 明细
- id_type=request：按 request_id 查 span 明细
- 详情仍返回 ObservabilityTraceQueryResp（list + total）
- task / event.publish / consumer / outbound.http / repository.db 等完整链路只在 detail 中展示，summary 只展示 root

## 完善定时器
定时任务统一走 task tracing 装饰器，root span 固定为 stage=task、kind=cron。任务内部可以继续深入到 service、DB、event.publish、consumer 甚至第三方 HTTP 调用。
1、对task进行拆包 h

## python集成完善
1、首先然ai生成一版 w3c的规范
2、然后查看一下调用时，请求体的具体效果
3、了解 是否是通过redis stream 具体的存储方法，同时讲同样的方法不同道python中，但是以go为主，pyhon只是推进内存+redisstream中，具体入库，依旧以go为主
4、设计一版pyhon改动效果

## 联通
彻底打通python与go之间的通路，彻底打通，并且同步上线。

